# アプリアイコン実装計画

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Android テンプレートのままのランチャーアイコンを、ポップでかわいいミツバチのアイコン一式（adaptive icon 3 レイヤー + レガシー mipmap 10 ファイル）に差し替える。

**Architecture:** 図形定義を Python の単一データ構造として持ち、そこから プレビュー用 SVG と 出荷用 VectorDrawable XML の両方を生成する。これによりレンダリングで検証した見た目と、実際にビルドへ入る XML の乖離を構造的に防ぐ。生成器と出力 SVG は `docs/superpowers/assets/icon/` に保管し、再生成可能にする。

**Tech Stack:** Python 3（標準ライブラリのみ）、`rsvg-convert`（SVG→PNG）、ImageMagick `magick`（マスク適用・webp 変換）、Android VectorDrawable

## Global Constraints

- 元仕様: `docs/superpowers/specs/2026-08-21-app-icon-design.md`
- キャンバスは 108×108（`viewportWidth` / `viewportHeight` ともに 108、`width` / `height` は 108dp）
- ミツバチ本体は中心 `(54, 54)` 基準で `scale(0.87)` を適用し、全要素を `r ≒ 32` 以内に収める
- 配色は仕様書の表から一切逸脱しない。輪郭 `#4A2A00`、目 `#3A2000`、頬 `#FF7A59`、羽根 `#FFFFFF`
- 背景グラデーション `#FFD98A` → `#FFA633`（放射）、胴体グラデーション `#FFE380` → `#FFC12E`（縦）
- リポジトリは PUBLIC。成果物に内部情報を含めない
- コミットメッセージは Conventional Commits 形式、`Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>` を付与する
- `git config commit.gpgsign` は `false` のため署名は不要
- 生成物はすべて生成器の出力とし、XML を手編集しない（手編集すると SVG と乖離し検証が無効になる）

---

## File Structure

| ファイル | 責務 |
| --- | --- |
| `docs/superpowers/assets/icon/icon_shapes.py` | 図形定義（背景・前景・monochrome の3レイヤー分）と、SVG / VectorDrawable 双方への出力関数 |
| `docs/superpowers/assets/icon/render_preview.sh` | SVG を PNG 化し、円 / スクワークルマスクと 48px 縮小を適用して検証シートを組む |
| `docs/superpowers/assets/icon/ic_launcher_background.svg` | 生成物（プレビュー用） |
| `docs/superpowers/assets/icon/ic_launcher_foreground.svg` | 生成物（プレビュー用） |
| `docs/superpowers/assets/icon/ic_launcher_monochrome.svg` | 生成物（プレビュー用） |
| `app/src/main/res/drawable/ic_launcher_background.xml` | 出荷物 |
| `app/src/main/res/drawable/ic_launcher_foreground.xml` | 出荷物 |
| `app/src/main/res/drawable/ic_launcher_monochrome.xml` | 出荷物（新規） |
| `app/src/main/res/mipmap-anydpi/ic_launcher.xml` | `<monochrome>` 参照先の変更 |
| `app/src/main/res/mipmap-anydpi/ic_launcher_round.xml` | 同上 |
| `app/src/main/res/mipmap-*/ic_launcher{,_round}.webp` | 出荷物（5密度 × 2種） |

---

## Task 1: 図形定義と SVG 生成器

**Files:**
- Create: `docs/superpowers/assets/icon/icon_shapes.py`
- Create: `docs/superpowers/assets/icon/render_preview.sh`
- Create: `docs/superpowers/assets/icon/ic_launcher_background.svg`（生成）
- Create: `docs/superpowers/assets/icon/ic_launcher_foreground.svg`（生成）

**Interfaces:**
- Produces:
  - `ellipse_path(cx, cy, rx, ry, cw=True) -> str`
  - `circle_path(cx, cy, r, cw=True) -> str`
  - `rrect_path(x, y, w, h, r, cw=True) -> str`
  - `hex_path(cx, cy, r) -> str`
  - `Shape` / `Group` / `Gradient` のデータクラス
  - `to_svg(layer) -> str`
  - `BACKGROUND`, `FOREGROUND` のレイヤー定義
- Task 2 は `to_vector_drawable(layer) -> str` をこのモジュールに追加する。Task 3 は `MONOCHROME` レイヤーを追加する。

- [ ] **Step 1: 生成器を作成する**

`docs/superpowers/assets/icon/icon_shapes.py`:

```python
"""Mitsubachi のランチャーアイコンの図形定義。

SVG（プレビュー用）と VectorDrawable（出荷用）を同一の定義から生成し、
検証した見た目と出荷物の乖離を防ぐ。
"""

from __future__ import annotations

import math
from dataclasses import dataclass, field

# ---------------------------------------------------------------- パス生成

def _arc(rx: float, ry: float, sweep: int, dx: float, dy: float) -> str:
    return f"a{rx},{ry} 0 1,{sweep} {dx},{dy}"


def ellipse_path(cx: float, cy: float, rx: float, ry: float, cw: bool = True) -> str:
    """楕円を 2 つの円弧で表現する。cw=False で逆回りになり、nonZero 塗りの穴になる。"""
    sweep = 1 if cw else 0
    return (
        f"M{cx - rx},{cy} "
        f"{_arc(rx, ry, sweep, 2 * rx, 0)} "
        f"{_arc(rx, ry, sweep, -2 * rx, 0)} Z"
    )


def circle_path(cx: float, cy: float, r: float, cw: bool = True) -> str:
    return ellipse_path(cx, cy, r, r, cw)


def rrect_path(x: float, y: float, w: float, h: float, r: float, cw: bool = True) -> str:
    """角丸矩形。cw=False で逆回りになる。"""
    if cw:
        return (
            f"M{x + r},{y} L{x + w - r},{y} a{r},{r} 0 0,1 {r},{r} "
            f"L{x + w},{y + h - r} a{r},{r} 0 0,1 {-r},{r} "
            f"L{x + r},{y + h} a{r},{r} 0 0,1 {-r},{-r} "
            f"L{x},{y + r} a{r},{r} 0 0,1 {r},{-r} Z"
        )
    return (
        f"M{x + r},{y} L{x},{y + r} a{r},{r} 0 0,0 {r},{-r} "
        f"L{x + r},{y + h} a{r},{r} 0 0,0 {-r},{-r} "
        f"L{x + w},{y + h - r} a{r},{r} 0 0,0 {-r},{r} "
        f"L{x + w - r},{y} a{r},{r} 0 0,0 {r},{r} Z"
    )


def hex_path(cx: float, cy: float, r: float) -> str:
    """頂点が上下に来る六角形。"""
    pts = []
    for i in range(6):
        angle = math.radians(90 + 60 * i)
        pts.append((cx + r * math.cos(angle), cy - r * math.sin(angle)))
    head = f"M{pts[0][0]:.2f},{pts[0][1]:.2f}"
    rest = " ".join(f"L{x:.2f},{y:.2f}" for x, y in pts[1:])
    return f"{head} {rest} Z"


# ---------------------------------------------------------------- モデル

@dataclass
class Gradient:
    kind: str  # "linear" | "radial"
    stops: list[tuple[float, str]]
    # linear
    x1: float = 0.0
    y1: float = 0.0
    x2: float = 0.0
    y2: float = 0.0
    # radial
    cx: float = 0.0
    cy: float = 0.0
    radius: float = 0.0


@dataclass
class Shape:
    d: str
    fill: str | None = None          # "#RRGGBB" または Gradient の名前 "grad:xxx"
    fill_alpha: float = 1.0
    fill_type: str = "nonZero"       # "nonZero" | "evenOdd"
    stroke: str | None = None
    stroke_width: float = 0.0
    stroke_alpha: float = 1.0
    cap: str = "butt"                # "butt" | "round"
    join: str = "miter"              # "miter" | "round"


@dataclass
class Group:
    children: list = field(default_factory=list)
    pivot: tuple[float, float] | None = None
    rotation: float = 0.0
    scale: float = 1.0
    clip: str | None = None


@dataclass
class Layer:
    name: str
    gradients: dict[str, Gradient] = field(default_factory=dict)
    children: list = field(default_factory=list)


# ---------------------------------------------------------------- SVG 出力

def _svg_paint(value: str | None) -> str:
    if value is None:
        return "none"
    if value.startswith("grad:"):
        return f"url(#{value[5:]})"
    return value


def _svg_node(node, indent: int) -> str:
    pad = "  " * indent
    if isinstance(node, Group):
        attrs = []
        transforms = []
        if node.scale != 1.0:
            px, py = node.pivot or (0.0, 0.0)
            transforms.append(f"translate({px} {py}) scale({node.scale}) translate({-px} {-py})")
        if node.rotation:
            px, py = node.pivot or (0.0, 0.0)
            transforms.append(f"rotate({node.rotation} {px} {py})")
        if transforms:
            attrs.append(f'transform="{" ".join(transforms)}"')
        clip_open = ""
        clip_close = ""
        if node.clip is not None:
            clip_id = f"clip{abs(hash(node.clip)) % 100000}"
            clip_open = (
                f'{pad}<clipPath id="{clip_id}"><path d="{node.clip}"/></clipPath>\n'
                f'{pad}<g clip-path="url(#{clip_id})">\n'
            )
            clip_close = f"{pad}</g>\n"
            indent += 1
            pad = "  " * indent
        inner = "".join(_svg_node(c, indent + 1) for c in node.children)
        body = f'{pad}<g {" ".join(attrs)}>\n{inner}{pad}</g>\n'
        return clip_open + body + clip_close
    attrs = [f'd="{node.d}"', f'fill="{_svg_paint(node.fill)}"']
    if node.fill_type == "evenOdd":
        attrs.append('fill-rule="evenodd"')
    if node.fill_alpha != 1.0:
        attrs.append(f'fill-opacity="{node.fill_alpha}"')
    if node.stroke is not None:
        attrs.append(f'stroke="{node.stroke}" stroke-width="{node.stroke_width}"')
        if node.stroke_alpha != 1.0:
            attrs.append(f'stroke-opacity="{node.stroke_alpha}"')
        if node.cap != "butt":
            attrs.append(f'stroke-linecap="{node.cap}"')
        if node.join != "miter":
            attrs.append(f'stroke-linejoin="{node.join}"')
    return f'{pad}<path {" ".join(attrs)}/>\n'


def to_svg(layer: Layer) -> str:
    defs = ""
    for name, g in layer.gradients.items():
        stops = "".join(
            f'      <stop offset="{o}" stop-color="{c}"/>\n' for o, c in g.stops
        )
        if g.kind == "linear":
            defs += (
                f'    <linearGradient id="{name}" gradientUnits="userSpaceOnUse" '
                f'x1="{g.x1}" y1="{g.y1}" x2="{g.x2}" y2="{g.y2}">\n{stops}'
                f"    </linearGradient>\n"
            )
        else:
            defs += (
                f'    <radialGradient id="{name}" gradientUnits="userSpaceOnUse" '
                f'cx="{g.cx}" cy="{g.cy}" r="{g.radius}">\n{stops}'
                f"    </radialGradient>\n"
            )
    body = "".join(_svg_node(c, 1) for c in layer.children)
    return (
        '<svg xmlns="http://www.w3.org/2000/svg" width="108" height="108" '
        'viewBox="0 0 108 108">\n'
        f"  <defs>\n{defs}  </defs>\n"
        f"{body}</svg>\n"
    )


# ---------------------------------------------------------------- レイヤー定義

OUTLINE = "#4A2A00"
EYE = "#3A2000"
BLUSH = "#FF7A59"
WING = "#FFFFFF"

BODY_CX, BODY_CY, BODY_RX, BODY_RY = 54.0, 58.0, 25.0, 23.0
BODY_PATH = ellipse_path(BODY_CX, BODY_CY, BODY_RX, BODY_RY)

_HEX_CENTERS = [
    (14, 10), (14, 42), (14, 74),
    (41.6, -6), (41.6, 26), (41.6, 58), (41.6, 90),
    (69.2, 10), (69.2, 42), (69.2, 74),
    (96.8, 26), (96.8, 58),
]

BACKGROUND = Layer(
    name="ic_launcher_background",
    gradients={
        "bg": Gradient(
            kind="radial", cx=54.0, cy=37.8, radius=91.8,
            stops=[(0.0, "#FFD98A"), (1.0, "#FFA633")],
        ),
    },
    children=[
        Shape(d=rrect_path(0, 0, 108, 108, 0.001), fill="grad:bg"),
        Group(children=[
            Shape(d=hex_path(cx, cy, 16), fill=None, stroke=WING,
                  stroke_width=2.6, stroke_alpha=0.30)
            for cx, cy in _HEX_CENTERS
        ]),
        Shape(d=circle_path(54, 55, 38), fill="#FFF6E0", fill_alpha=0.55),
    ],
)

_BEE = [
    # 触角（線）
    Shape(d="M46,39 C43,32 41,29 39,27.5", fill=None, stroke=OUTLINE,
          stroke_width=3.2, cap="round"),
    Shape(d="M62,39 C65,32 67,29 69,27.5", fill=None, stroke=OUTLINE,
          stroke_width=3.2, cap="round"),
    # 触角（先端の玉）
    Shape(d=circle_path(38, 26.5, 3.2), fill=OUTLINE),
    Shape(d=circle_path(70, 26.5, 3.2), fill=OUTLINE),
    # 羽根
    Group(pivot=(33, 43), rotation=-28, children=[
        Shape(d=ellipse_path(33, 43, 12, 8.5), fill=WING, fill_alpha=0.92,
              stroke=OUTLINE, stroke_width=3.4),
    ]),
    Group(pivot=(75, 43), rotation=28, children=[
        Shape(d=ellipse_path(75, 43, 12, 8.5), fill=WING, fill_alpha=0.92,
              stroke=OUTLINE, stroke_width=3.4),
    ]),
    # 胴体
    Shape(d=BODY_PATH, fill="grad:body"),
    # 縞（胴体でクリップ）
    Group(clip=BODY_PATH, children=[
        Group(pivot=(BODY_CX, BODY_CY), rotation=-5, children=[
            Shape(d=rrect_path(24, 62, 60, 8, 4), fill=OUTLINE),
            Shape(d=rrect_path(24, 74, 60, 8, 4), fill=OUTLINE),
        ]),
    ]),
    # 胴体の輪郭
    Shape(d=BODY_PATH, fill=None, stroke=OUTLINE, stroke_width=4),
    # 顔
    Shape(d=ellipse_path(37.5, 57, 5, 3.2), fill=BLUSH, fill_alpha=0.6),
    Shape(d=ellipse_path(70.5, 57, 5, 3.2), fill=BLUSH, fill_alpha=0.6),
    Shape(d=circle_path(45.5, 50, 4.3), fill=EYE),
    Shape(d=circle_path(62.5, 50, 4.3), fill=EYE),
    Shape(d=circle_path(44.1, 48.4, 1.7), fill=WING),
    Shape(d=circle_path(61.1, 48.4, 1.7), fill=WING),
    Shape(d="M49.5,56.5 Q54,61 58.5,56.5", fill=None, stroke=EYE,
          stroke_width=2.6, cap="round"),
]

FOREGROUND = Layer(
    name="ic_launcher_foreground",
    gradients={
        "body": Gradient(
            kind="linear", x1=54.0, y1=35.0, x2=54.0, y2=81.0,
            stops=[(0.0, "#FFE380"), (1.0, "#FFC12E")],
        ),
    },
    children=[Group(pivot=(54, 54), scale=0.87, children=_BEE)],
)

LAYERS = [BACKGROUND, FOREGROUND]


if __name__ == "__main__":
    import pathlib
    import sys

    out_dir = pathlib.Path(sys.argv[1] if len(sys.argv) > 1 else ".")
    for layer in LAYERS:
        (out_dir / f"{layer.name}.svg").write_text(to_svg(layer), encoding="utf-8")
        print(f"wrote {layer.name}.svg")
```

- [ ] **Step 2: SVG を生成する**

```bash
cd docs/superpowers/assets/icon && python3 icon_shapes.py .
```

期待する出力:

```
wrote ic_launcher_background.svg
wrote ic_launcher_foreground.svg
```

- [ ] **Step 3: 検証スクリプトを作成する**

`docs/superpowers/assets/icon/render_preview.sh`:

```bash
#!/usr/bin/env bash
# 背景と前景を合成し、円 / スクワークルマスクと 48px 縮小を適用した検証シートを作る。
# usage: ./render_preview.sh <out_dir>
set -euo pipefail

dir="$(cd "$(dirname "$0")" && pwd)"
out="${1:-$dir/preview}"
mkdir -p "$out"

rsvg-convert -w 432 -h 432 "$dir/ic_launcher_background.svg" -o "$out/bg.png"
rsvg-convert -w 432 -h 432 "$dir/ic_launcher_foreground.svg" -o "$out/fg.png"
magick "$out/bg.png" "$out/fg.png" -compose over -composite "$out/full.png"

# 108dp のうち中央 72dp のみが表示される
crop() {
  magick "$out/full.png" -resize 648x648 -gravity center -crop 432x432+0+0 +repage "$1"
}
crop "$out/visible.png"

magick "$out/visible.png" \
  \( -size 432x432 xc:none -fill white -draw "circle 216,216 216,4" \) \
  -alpha set -compose DstIn -composite "$out/mask_circle.png"
magick "$out/visible.png" \
  \( -size 432x432 xc:none -fill white -draw "roundrectangle 0,0 431,431 118,118" \) \
  -alpha set -compose DstIn -composite "$out/mask_squircle.png"
magick "$out/mask_circle.png" -resize 48x48 "$out/size48.png"
magick "$out/size48.png" -resize 432x432 "$out/size48_zoom.png"

magick \( "$out/full.png" "$out/mask_circle.png" +append \) \
       \( "$out/mask_squircle.png" "$out/size48_zoom.png" +append \) \
       -background '#FAFAFA' -append -bordercolor '#FAFAFA' -border 16 "$out/sheet.png"
echo "wrote $out/sheet.png"
```

- [ ] **Step 4: 検証シートを生成して目視確認する**

```bash
chmod +x docs/superpowers/assets/icon/render_preview.sh && docs/superpowers/assets/icon/render_preview.sh
```

期待する出力: `wrote .../preview/sheet.png`

生成された `sheet.png` を開き、次の 3 点を確認する。いずれかを満たさない場合は Task 1 Step 1 の座標を調整して再生成する。

1. 円マスク（右上）で触角の先端の玉と羽根の外端が欠けていない
2. スクワークルマスク（左下）でも同様に欠けていない
3. 48px 拡大（右下）で目・口・頬・縞が判別できる

- [ ] **Step 5: コミット**

```bash
git add docs/superpowers/assets/icon
git commit -m "$(cat <<'EOF'
feat(icon): add icon shape generator and preview renderer

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
EOF
)"
```

---

## Task 2: VectorDrawable 出力と背景・前景の差し替え

**Files:**
- Modify: `docs/superpowers/assets/icon/icon_shapes.py`（`to_vector_drawable` を追加、`__main__` を拡張）
- Modify: `app/src/main/res/drawable/ic_launcher_background.xml`（全面差し替え）
- Modify: `app/src/main/res/drawable/ic_launcher_foreground.xml`（全面差し替え）

**Interfaces:**
- Consumes: Task 1 の `Layer` / `Group` / `Shape` / `Gradient`、`BACKGROUND`、`FOREGROUND`
- Produces: `to_vector_drawable(layer) -> str`

- [ ] **Step 1: VectorDrawable 出力関数を追加する**

`icon_shapes.py` の `# ------- レイヤー定義` の直前に挿入する:

```python
# ---------------------------------------------------------------- VD 出力

_VD_CAP = {"butt": "butt", "round": "round"}
_VD_JOIN = {"miter": "miter", "round": "round"}


def _vd_gradient(g: Gradient, indent: int) -> str:
    pad = "  " * indent
    if g.kind == "linear":
        head = (
            f'{pad}<gradient android:type="linear" '
            f'android:startX="{g.x1}" android:startY="{g.y1}" '
            f'android:endX="{g.x2}" android:endY="{g.y2}">'
        )
    else:
        head = (
            f'{pad}<gradient android:type="radial" '
            f'android:centerX="{g.cx}" android:centerY="{g.cy}" '
            f'android:gradientRadius="{g.radius}">'
        )
    items = "".join(
        f'{pad}  <item android:color="{c}" android:offset="{o}"/>\n' for o, c in g.stops
    )
    return f"{head}\n{items}{pad}</gradient>\n"


def _vd_node(node, layer: Layer, indent: int) -> str:
    pad = "  " * indent
    if isinstance(node, Group):
        attrs = []
        if node.pivot is not None and (node.scale != 1.0 or node.rotation):
            attrs.append(f'android:pivotX="{node.pivot[0]}"')
            attrs.append(f'android:pivotY="{node.pivot[1]}"')
        if node.scale != 1.0:
            attrs.append(f'android:scaleX="{node.scale}"')
            attrs.append(f'android:scaleY="{node.scale}"')
        if node.rotation:
            attrs.append(f'android:rotation="{node.rotation}"')
        clip = ""
        if node.clip is not None:
            clip = f'{pad}  <clip-path android:pathData="{node.clip}"/>\n'
        inner = "".join(_vd_node(c, layer, indent + 1) for c in node.children)
        joined = ("\n" + pad + "    ").join(attrs) if attrs else ""
        open_tag = f"{pad}<group {joined}>" if attrs else f"{pad}<group>"
        return f"{open_tag}\n{clip}{inner}{pad}</group>\n"

    attrs = [f'android:pathData="{node.d}"']
    gradient_child = None
    if node.fill is not None:
        if node.fill.startswith("grad:"):
            gradient_child = layer.gradients[node.fill[5:]]
        else:
            attrs.append(f'android:fillColor="{node.fill}"')
    else:
        attrs.append('android:fillColor="#00000000"')
    if node.fill_type == "evenOdd":
        attrs.append('android:fillType="evenOdd"')
    if node.fill_alpha != 1.0:
        attrs.append(f'android:fillAlpha="{node.fill_alpha}"')
    if node.stroke is not None:
        attrs.append(f'android:strokeColor="{node.stroke}"')
        attrs.append(f'android:strokeWidth="{node.stroke_width}"')
        if node.stroke_alpha != 1.0:
            attrs.append(f'android:strokeAlpha="{node.stroke_alpha}"')
        if node.cap != "butt":
            attrs.append(f'android:strokeLineCap="{_VD_CAP[node.cap]}"')
        if node.join != "miter":
            attrs.append(f'android:strokeLineJoin="{_VD_JOIN[node.join]}"')

    joined = ("\n" + pad + "    ").join(attrs)
    if gradient_child is None:
        return f"{pad}<path {joined}/>\n"
    grad = _vd_gradient(gradient_child, indent + 2)
    return (
        f"{pad}<path {joined}>\n"
        f'{pad}  <aapt:attr name="android:fillColor">\n'
        f"{grad}"
        f"{pad}  </aapt:attr>\n"
        f"{pad}</path>\n"
    )


def to_vector_drawable(layer: Layer) -> str:
    body = "".join(_vd_node(c, layer, 1) for c in layer.children)
    return (
        '<?xml version="1.0" encoding="utf-8"?>\n'
        '<vector xmlns:android="http://schemas.android.com/apk/res/android"\n'
        '    xmlns:aapt="http://schemas.android.com/aapt"\n'
        '    android:width="108dp"\n'
        '    android:height="108dp"\n'
        '    android:viewportWidth="108"\n'
        '    android:viewportHeight="108">\n'
        f"{body}"
        "</vector>\n"
    )
```

- [ ] **Step 2: `__main__` を XML も書き出すよう差し替える**

`icon_shapes.py` 末尾の `if __name__ == "__main__":` ブロックを次に置き換える:

```python
if __name__ == "__main__":
    import pathlib
    import sys

    out_dir = pathlib.Path(sys.argv[1] if len(sys.argv) > 1 else ".")
    res_dir = pathlib.Path(sys.argv[2]) if len(sys.argv) > 2 else None
    for layer in LAYERS:
        (out_dir / f"{layer.name}.svg").write_text(to_svg(layer), encoding="utf-8")
        print(f"wrote {layer.name}.svg")
        if res_dir is not None:
            (res_dir / f"{layer.name}.xml").write_text(
                to_vector_drawable(layer), encoding="utf-8"
            )
            print(f"wrote {layer.name}.xml")
```

- [ ] **Step 3: 生成して差し替える**

```bash
cd docs/superpowers/assets/icon && python3 icon_shapes.py . ../../../../app/src/main/res/drawable
```

期待する出力:

```
wrote ic_launcher_background.svg
wrote ic_launcher_background.xml
wrote ic_launcher_foreground.svg
wrote ic_launcher_foreground.xml
```

- [ ] **Step 4: 生成された XML が VectorDrawable として妥当か確認する**

```bash
xmllint --noout app/src/main/res/drawable/ic_launcher_background.xml app/src/main/res/drawable/ic_launcher_foreground.xml && echo "XML OK"
```

期待する出力: `XML OK`

さらに、テンプレート由来の記述が残っていないことを確認する:

```bash
grep -c "3DDC84" app/src/main/res/drawable/ic_launcher_background.xml || echo "template color removed"
```

期待する出力: `template color removed`（テンプレートの緑 `#3DDC84` が残っていない）

- [ ] **Step 5: コミット**

```bash
git add docs/superpowers/assets/icon app/src/main/res/drawable/ic_launcher_background.xml app/src/main/res/drawable/ic_launcher_foreground.xml
git commit -m "$(cat <<'EOF'
feat(icon): replace launcher background and foreground with bee artwork

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
EOF
)"
```

---

## Task 3: monochrome レイヤー

**Files:**
- Modify: `docs/superpowers/assets/icon/icon_shapes.py`（`MONOCHROME` を追加、`LAYERS` に登録）
- Modify: `docs/superpowers/assets/icon/render_preview.sh`（monochrome の確認を追加）
- Create: `app/src/main/res/drawable/ic_launcher_monochrome.xml`（生成）
- Modify: `app/src/main/res/mipmap-anydpi/ic_launcher.xml`
- Modify: `app/src/main/res/mipmap-anydpi/ic_launcher_round.xml`

**Interfaces:**
- Consumes: Task 1 の `ellipse_path` / `circle_path` / `rrect_path`、`BODY_PATH`、`_BEE` の座標
- Produces: `MONOCHROME: Layer`

テーマアイコンは drawable のアルファのみを使って単色で塗り潰す。したがってカラー版の前景を流用すると顔も縞も消える。目・口・縞は **逆回りのサブパスによる nonZero の穴** として表現する（`evenOdd` にすると羽根と胴体の重なりまで穴になるため使わない）。触角と羽根は穴を持たないので、胴体とは別のパスに分ける。

- [ ] **Step 1: `MONOCHROME` レイヤーを追加する**

`icon_shapes.py` の `LAYERS = [BACKGROUND, FOREGROUND]` の直前に挿入する:

```python
_MONO = "#FFFFFF"

# 口は線ではなく塗りの帯として表現する（単色化すると細線が消えるため太らせる）
_MOUTH_HOLE = (
    "M48.6,55.6 Q54,62 59.4,55.6 L62.2,58.0 Q54,66.4 45.8,58.0 Z"
)

_MONOCHROME_BEE = [
    # 触角（線）と先端の玉 — 穴を持たないので独立したパス
    Shape(d="M46,39 C43,32 41,29 39,27.5", fill=None, stroke=_MONO,
          stroke_width=3.6, cap="round"),
    Shape(d="M62,39 C65,32 67,29 69,27.5", fill=None, stroke=_MONO,
          stroke_width=3.6, cap="round"),
    Shape(d=circle_path(38, 26.5, 3.4), fill=_MONO),
    Shape(d=circle_path(70, 26.5, 3.4), fill=_MONO),
    # 羽根 — 胴体の下に敷く。穴を持たない
    Group(pivot=(33, 43), rotation=-28, children=[
        Shape(d=ellipse_path(33, 43, 12, 8.5), fill=_MONO),
    ]),
    Group(pivot=(75, 43), rotation=28, children=[
        Shape(d=ellipse_path(75, 43, 12, 8.5), fill=_MONO),
    ]),
    # 胴体 + 抜き穴（逆回り）
    Shape(
        d=" ".join([
            ellipse_path(BODY_CX, BODY_CY, BODY_RX, BODY_RY),
            circle_path(45.5, 50, 5.0, cw=False),
            circle_path(62.5, 50, 5.0, cw=False),
            _MOUTH_HOLE,
            rrect_path(30, 63.5, 48, 6.5, 3.25, cw=False),
            rrect_path(33, 74.5, 42, 6.0, 3.0, cw=False),
        ]),
        fill=_MONO,
    ),
]

MONOCHROME = Layer(
    name="ic_launcher_monochrome",
    children=[Group(pivot=(54, 54), scale=0.87, children=_MONOCHROME_BEE)],
)
```

`_MOUTH_HOLE` は時計回りに描かれているため、そのままでは穴にならない。次のとおり反転版に差し替える:

```python
_MOUTH_HOLE = (
    "M45.8,58.0 Q54,66.4 62.2,58.0 L59.4,55.6 Q54,62 48.6,55.6 Z"
)
```

- [ ] **Step 2: `LAYERS` に登録する**

```python
LAYERS = [BACKGROUND, FOREGROUND, MONOCHROME]
```

- [ ] **Step 3: 生成する**

```bash
cd docs/superpowers/assets/icon && python3 icon_shapes.py . ../../../../app/src/main/res/drawable
```

期待する出力に `wrote ic_launcher_monochrome.svg` と `wrote ic_launcher_monochrome.xml` が含まれること。

- [ ] **Step 4: monochrome のシルエット検証を追加する**

`render_preview.sh` の `echo "wrote $out/sheet.png"` の直前に挿入する:

```bash
# monochrome: 実機と同様にアルファのみを使って単色で塗り潰した状態を再現する
rsvg-convert -w 432 -h 432 "$dir/ic_launcher_monochrome.svg" -o "$out/mono_raw.png"
magick "$out/mono_raw.png" -alpha extract -background '#1B1B1B' -alpha shape \
  -fill '#E8C48A' -colorize 100 "$out/mono_shape.png"
magick -size 432x432 xc:'#1B1B1B' "$out/mono_shape.png" -compose over -composite \
  -resize 648x648 -gravity center -crop 432x432+0+0 +repage \
  \( -size 432x432 xc:none -fill white -draw "circle 216,216 216,4" \) \
  -alpha set -compose DstIn -composite "$out/mono_circle.png"
magick "$out/mono_circle.png" -resize 48x48 -resize 432x432 "$out/mono_48.png"
magick "$out/mono_circle.png" "$out/mono_48.png" +append \
  -bordercolor '#FAFAFA' -border 16 "$out/sheet_mono.png"
echo "wrote $out/sheet_mono.png"
```

- [ ] **Step 5: monochrome を目視確認する**

```bash
docs/superpowers/assets/icon/render_preview.sh
```

`preview/sheet_mono.png` を開き、次を確認する。満たさない場合は Step 1 の穴の寸法を太らせて再生成する。

1. 単色に潰しても 目 2 つ・口・縞 2 本 が穴として残っている
2. 48px（右）でも目と口が判別できる
3. 触角と羽根が胴体と地続きの 1 つのシルエットに見える

- [ ] **Step 6: adaptive-icon の参照先を差し替える**

`app/src/main/res/mipmap-anydpi/ic_launcher.xml` と `app/src/main/res/mipmap-anydpi/ic_launcher_round.xml` の両方について、次の 1 行を

```xml
    <monochrome android:drawable="@drawable/ic_launcher_foreground" />
```

次に置き換える。

```xml
    <monochrome android:drawable="@drawable/ic_launcher_monochrome" />
```

コマンドで行う場合:

```bash
sed -i 's|<monochrome android:drawable="@drawable/ic_launcher_foreground" />|<monochrome android:drawable="@drawable/ic_launcher_monochrome" />|' app/src/main/res/mipmap-anydpi/ic_launcher.xml app/src/main/res/mipmap-anydpi/ic_launcher_round.xml
```

- [ ] **Step 7: 参照が正しいことを確認する**

```bash
grep -h monochrome app/src/main/res/mipmap-anydpi/ic_launcher.xml app/src/main/res/mipmap-anydpi/ic_launcher_round.xml
```

期待する出力（2 行とも同一）:

```
    <monochrome android:drawable="@drawable/ic_launcher_monochrome" />
```

- [ ] **Step 8: コミット**

```bash
git add docs/superpowers/assets/icon app/src/main/res/drawable/ic_launcher_monochrome.xml app/src/main/res/mipmap-anydpi
git commit -m "$(cat <<'EOF'
feat(icon): add dedicated monochrome layer for themed icons

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
EOF
)"
```

---

## Task 4: レガシー mipmap の再生成

**Files:**
- Create: `docs/superpowers/assets/icon/generate_mipmaps.sh`
- Modify: `app/src/main/res/mipmap-{mdpi,hdpi,xhdpi,xxhdpi,xxxhdpi}/ic_launcher.webp`
- Modify: `app/src/main/res/mipmap-{mdpi,hdpi,xhdpi,xxhdpi,xxxhdpi}/ic_launcher_round.webp`

**Interfaces:**
- Consumes: Task 1 で生成した `ic_launcher_background.svg` / `ic_launcher_foreground.svg`

密度と寸法は既存ファイルに合わせる。mdpi 48 / hdpi 72 / xhdpi 96 / xxhdpi 144 / xxxhdpi 192。
`ic_launcher` はスクワークル、`ic_launcher_round` は円形でマスクする。

- [ ] **Step 1: 生成スクリプトを作成する**

`docs/superpowers/assets/icon/generate_mipmaps.sh`:

```bash
#!/usr/bin/env bash
# レガシー mipmap（adaptive icon 非対応ランチャー向け）を SVG から再生成する。
set -euo pipefail

dir="$(cd "$(dirname "$0")" && pwd)"
res="$dir/../../../../app/src/main/res"
tmp="$(mktemp -d)"
trap 'rm -rf "$tmp"' EXIT

rsvg-convert -w 768 -h 768 "$dir/ic_launcher_background.svg" -o "$tmp/bg.png"
rsvg-convert -w 768 -h 768 "$dir/ic_launcher_foreground.svg" -o "$tmp/fg.png"
magick "$tmp/bg.png" "$tmp/fg.png" -compose over -composite \
  -resize 1152x1152 -gravity center -crop 768x768+0+0 +repage "$tmp/visible.png"

magick "$tmp/visible.png" \
  \( -size 768x768 xc:none -fill white -draw "roundrectangle 0,0 767,767 210,210" \) \
  -alpha set -compose DstIn -composite "$tmp/squircle.png"
magick "$tmp/visible.png" \
  \( -size 768x768 xc:none -fill white -draw "circle 384,384 384,4" \) \
  -alpha set -compose DstIn -composite "$tmp/circle.png"

emit() { # $1=density $2=size
  magick "$tmp/squircle.png" -resize "$2x$2" -define webp:lossless=true \
    "$res/mipmap-$1/ic_launcher.webp"
  magick "$tmp/circle.png" -resize "$2x$2" -define webp:lossless=true \
    "$res/mipmap-$1/ic_launcher_round.webp"
  echo "wrote mipmap-$1 (${2}px)"
}

emit mdpi 48
emit hdpi 72
emit xhdpi 96
emit xxhdpi 144
emit xxxhdpi 192
```

- [ ] **Step 2: 実行する**

```bash
chmod +x docs/superpowers/assets/icon/generate_mipmaps.sh && docs/superpowers/assets/icon/generate_mipmaps.sh
```

期待する出力:

```
wrote mipmap-mdpi (48px)
wrote mipmap-hdpi (72px)
wrote mipmap-xhdpi (96px)
wrote mipmap-xxhdpi (144px)
wrote mipmap-xxxhdpi (192px)
```

- [ ] **Step 3: 寸法が既存と一致することを確認する**

```bash
for f in $(find app/src/main/res -name "ic_launcher*.webp" | sort); do printf "%-52s " "$f"; identify -format "%wx%h %m\n" "$f"; done
```

期待する出力: 10 行すべてが `WEBP` 形式で、mdpi=48x48 / hdpi=72x72 / xhdpi=96x96 / xxhdpi=144x144 / xxxhdpi=192x192 であること。

- [ ] **Step 4: コミット**

```bash
git add docs/superpowers/assets/icon/generate_mipmaps.sh app/src/main/res/mipmap-mdpi app/src/main/res/mipmap-hdpi app/src/main/res/mipmap-xhdpi app/src/main/res/mipmap-xxhdpi app/src/main/res/mipmap-xxxhdpi
git commit -m "$(cat <<'EOF'
feat(icon): regenerate legacy mipmap assets from bee artwork

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
EOF
)"
```

---

## Task 5: 総合検証と証跡

**Files:**
- Create: `docs/superpowers/assets/icon/preview/before_after.png`（証跡）

- [ ] **Step 1: before の画像を取得する**

差し替え前のアイコンを git から復元してレンダリングする。

```bash
mkdir -p /tmp/icon_before && git show HEAD~4:app/src/main/res/mipmap-xxxhdpi/ic_launcher.webp > /tmp/icon_before/before.webp && magick /tmp/icon_before/before.webp -resize 432x432 /tmp/icon_before/before.png && identify /tmp/icon_before/before.png
```

期待する出力: `... PNG 432x432 ...`

`HEAD~4` は Task 1〜4 の 4 コミットを遡る指定。コミット数が異なる場合は `git log --oneline` で差し替え前のコミットを特定して使う。

- [ ] **Step 2: before / after の比較画像を作る**

```bash
magick /tmp/icon_before/before.png docs/superpowers/assets/icon/preview/mask_squircle.png +append -bordercolor '#FAFAFA' -border 16 docs/superpowers/assets/icon/preview/before_after.png && identify docs/superpowers/assets/icon/preview/before_after.png
```

期待する出力: `... PNG 896x464 ...`

- [ ] **Step 3: Gradle 検証を試行する**

```bash
./gradlew :app:lintDebug --offline 2>&1 | tail -30
```

`secrets.properties` / `app/google-services.json` / `keystore.properties` が未配置のため失敗する可能性が高い。失敗した場合はエラー出力をそのまま記録し、原因がアイコン変更ではなくビルド前提の不足であることを確認する。到達できた場合は、アイコン起因の lint 指摘がないことを確認する。

アイコン単体の妥当性は次でも確認できる:

```bash
for f in app/src/main/res/drawable/ic_launcher_*.xml app/src/main/res/mipmap-anydpi/*.xml; do xmllint --noout "$f" && echo "OK $f"; done
```

期待する出力: 5 ファイルすべてに `OK` が付くこと。

- [ ] **Step 4: 検証結果をまとめてコミットする**

```bash
git add docs/superpowers/assets/icon/preview/before_after.png
git commit -m "$(cat <<'EOF'
docs(icon): add before/after evidence for the launcher icon

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
EOF
)"
```

- [ ] **Step 5: 報告する**

次を明示して報告する。

- 円 / スクワークル / 48px / monochrome の各検証結果
- Gradle 検証が到達できた範囲と、到達できなかった場合はその理由（ビルド前提の不足）
- 実機・エミュレータでのランチャー表示が未検証である旨

ビルドが通らず実機検証ができない場合は、未検証項目が残るため PR は Draft で作成する。

---

## Self-Review

**仕様カバレッジ**

| 仕様の要求 | 対応タスク |
| --- | --- |
| モチーフと造形（丸い胴体・顔・触角・羽根・濃茶の輪郭） | Task 1 Step 1 の `_BEE` |
| 配色表 | Task 1 Step 1 の定数と `Gradient` |
| 背景ハニカム（opacity 0.30） | Task 1 Step 1 の `BACKGROUND` |
| レイヤー構成（中央の円は background） | Task 1 Step 1 の `BACKGROUND` |
| セーフゾーン（`scale(0.87)`、両マスクで欠けない） | Task 1 Step 4 |
| monochrome 専用 drawable、目・口・縞を穴に | Task 3 |
| `<monochrome>` 参照先の差し替え | Task 3 Step 6 |
| 成果物一覧（drawable 3 / anydpi 2 / mipmap 10） | Task 2・3・4 |
| SVG 原本を `docs/superpowers/assets/icon/` に保管 | Task 1 Step 5 |
| VectorDrawable を手書きしない | Task 2（生成器経由） |
| 検証の優先順（レンダリング → Gradle → 実機） | Task 1 Step 4、Task 3 Step 5、Task 5 |
| before / after の証跡 | Task 5 Step 2 |

**型・名称の整合**

- `ellipse_path` / `circle_path` / `rrect_path` / `hex_path` は Task 1 で定義し、Task 3 で同名のまま利用している。
- `BODY_PATH` / `BODY_CX` / `BODY_CY` / `BODY_RX` / `BODY_RY` は Task 1 で定義し、Task 3 で参照している。
- `to_svg` / `to_vector_drawable` はいずれも `Layer` を受け取り `str` を返す。
- `LAYERS` は Task 1 で 2 要素、Task 3 Step 2 で 3 要素に更新される。

**仕様との差分**

- 仕様の「制作フロー」は SVG を作ってから VectorDrawable へ「変換」すると書いているが、本計画では単一定義からの「同時生成」に強めた。検証対象と出荷物の一致を保証するためであり、仕様の意図（手書きしない・再生成可能にする）を満たす。
